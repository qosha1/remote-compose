# Copy to .env for local docker-compose (gitignored).
# For rc deploy, rc.yml's secrets section references .env.production
# instead, and `rc secrets push` uploads values to AWS Secrets Manager.
APP_SECRET_KEY=dev-not-for-prod
APP_VERSION=dev

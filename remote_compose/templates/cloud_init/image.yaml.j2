#cloud-config
package_update: true
packages:
  - docker

runcmd:
  - systemctl enable --now docker
  - usermod -aG docker ec2-user
  - docker pull {{ image }}
  - docker run -d --restart=unless-stopped --name rc-dev-app {{ image }}

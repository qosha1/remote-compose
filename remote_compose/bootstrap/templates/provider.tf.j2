# Region + credentials come from the environment / profile (AWS_REGION,
# AWS_PROFILE), the same as any terraform stack. IAM is global; the region only
# scopes the codebuild/ecr/ecs ARNs via data.aws_region.
provider "aws" {}

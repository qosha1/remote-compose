"""rc v1 → rc v2 migration tooling (in-place, zero-data-loss).

Mirrors the structure of remote_compose/copilot/ — discover the live
state, translate per-resource, build a plan, apply phases.

NOT YET IMPLEMENTED — failing-test scaffolding only.
"""
